# Scheduler recorded fixtures

These fixtures contain synthetic, bounded provider evidence for deterministic
tests and evals. They contain no credentials, real household records, network
transport, or approval authority. A query absent from the recordings must
remain unavailable; it must never be inferred from a nearby fixture.

Live Google Calendar and Maps smoke tests are intentionally deferred until
deployment credentials and isolated provider test resources are available.
