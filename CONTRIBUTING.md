# Contributing

Keep changes scoped to a planned task. Begin every behaviour change with a
focused failing test, implement the smallest passing change, and run lint,
typecheck, and the relevant test suite before committing.

Do not add credentials, production data, provider tokens, or generated local
configuration to the repository.
